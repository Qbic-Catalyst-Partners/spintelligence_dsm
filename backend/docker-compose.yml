services:
  backend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: spintelligence-backend
    restart: unless-stopped
    env_file:
      - .env
    ports:
      - "4000:4000"
    volumes:
      - ./uploads:/app/uploads
